;; use ctr-h as backspace key
(global-set-key "\C-h" 'delete-backward-char)

;; display line number
(global-linum-mode t)

;; use theme in emacs24
(load-theme 'monokai t)


(setq x-select-enable-clipboard t)

